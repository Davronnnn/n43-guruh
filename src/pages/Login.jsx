import { Card, CardHeader, CardMedia, Pagination } from '@mui/material';
import Button from '@mui/material/Button';

export default function Login() {
	return <Pagination count={10} color='primary' />;
}
